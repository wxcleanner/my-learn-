#include<iostream>
using namespace std;
#include<numeric>
#include<vector>
void test()
{
	vector<int>v;
	for (int i = 0; i <= 100; i++)
	{
		v.push_back(i);
	}
	int total = accumulate(v.begin(), v.end(), 50);
cout << total << endl;
}
int main()
{
	test();
	return 0;
}
